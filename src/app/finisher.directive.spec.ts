import { FinisherDirective } from './finisher.directive';

describe('FinisherDirective', () => {
  it('should create an instance', () => {
    const directive = new FinisherDirective();
    expect(directive).toBeTruthy();
  });
});
